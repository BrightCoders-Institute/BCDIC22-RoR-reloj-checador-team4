<%= form_with model: @company do |form|  %>
  <div>
    <%= form.label :Name %>
    <%= form.text_field :title, placeholder: "Company's Name" %>
  </div>
  <div>
    <%= form.label :Address %>
    <%= form.text_field :body, placeholder: "Company's Address"%>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
